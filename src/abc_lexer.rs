/// ABC Lexer
/// Transform strings of ABC into a sequence of lexed tokens.
/// This accepts a String and returns newly allocated strings that have an independent lifetime to the supplied string.

// Placeholder
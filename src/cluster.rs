/// Cluster
/// Cluster tunes into groups that are similar, so that we can group very similar tunes (e.g. different transcriptions of the same tune) as one.
/// Uses both geometric and ngram tune representations.

/// Placeholder.